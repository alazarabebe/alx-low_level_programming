#include "main.h"

#include <ctype.h>

/**
 * _isupper - check if its upper case
 * @c: input char
 * Return: integer
 */

int _isdigit(int c)
{
	if (c >= 48 && c < 58)
	{
	return (1);
	}
	return (0);
}
